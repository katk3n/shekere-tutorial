# 演習 4: 最終調整とエフェクト

## 目標
「太陽の輪」作品を完成させるため、最終的な調整とエフェクトを追加する。

## 学習内容
- **色のブレンド**: `mix()`関数による高度な色合成
- **エッジ処理**: `smoothstep()`関数による滑らかなグラデーション
- **特殊効果**: 中心の白い円と縁のフェード効果

## 課題の流れ
この演習では、「太陽の輪」作品の最終ステップとして、中心の白い円と縁のフェード効果を追加し、作品を完成させます。

## 実装手順

### ステップ1: 前回のコードから開始
まず、前回作成した放射状パターン付きの同心円のコードを基に進めます。

```wgsl
@fragment
fn fs_main(in: VertexOutput) -> @location(0) vec4<f32> {
    let uv = NormalizedCoords(in.position.xy);
    let distance = length(uv);
    let angle = atan2(uv.y, uv.x);
    
    var color = vec3(0.0);
    
    // 同心円パターン
    if distance < 0.3 {
        color = vec3(1.0, 0.3, 0.1);
    } else if distance < 0.6 {
        color = vec3(1.0, 0.8, 0.0);
    } else if distance < 0.9 {
        color = vec3(1.0, 0.5, 0.0);
    }
    
    // 放射状パターン
    let radial_lines = sin(angle * 4.0) * 0.5 + 0.5;
    let line_intensity = step(0.7, radial_lines);
    
    if line_intensity > 0.5 && distance > 0.1 {
        color = mix(color, vec3(1.0), 0.6);
    }
    
    return vec4(ToLinearRgb(color), 1.0);
}
```

### ステップ2: 中心に白い円を追加
太陽の核心部分として、中心に小さな白い円を追加します。

```wgsl
// ... 前の部分は同じ ...

// 中心に小さな白い円を追加
if distance < 0.1 {
    color = vec3(1.0, 1.0, 1.0);
}
```

### ステップ3: 縁のフェード効果を追加
外側の境界を滑らかにするため、`smoothstep()`を使用します。

```wgsl
// ... 前の部分は同じ ...

// 縁のグラデーションで柔らかさを追加
let edge_fade = smoothstep(0.95, 1.0, distance);
color = mix(color, vec3(0.0), edge_fade);
```

### ステップ4: 背景色を改善
宇宙をイメージした深い紫色の背景に変更します。

```wgsl
// ... 同心円パターンの部分を修正 ...
} else {
    // 背景: 深い紫
    color = vec3(0.2, 0.1, 0.4);
}
```

### ステップ5: 完成コードの確認
全ての要素を統合したコードになることを確認しましょう。

## 完成目標
以下の要素がすべて含まれた「太陽の輪」が表示されることを確認してください：
- 3層の同心円（赤・黄・オレンジ）
- 8方向の白い放射状ライン
- 中心の白い円
- 深い紫色の背景
- 外側の滑らかなフェード効果

## 作品の完成！
おめでとうございます！これで「太陽の輪」作品が完成しました。

## 学習のポイント
- `smoothstep(min, max, x)`で滑らかなグラデーションを作れます
- 複数の効果を段階的に重ね合わせることで複雑な表現ができます
- 色の選択が作品の雰囲気を大きく左右します
- 中心の特別処理で焦点を作ることができます

## 発展課題
余裕があれば以下にも挑戦してみてください：
1. 背景色を変更して雰囲気を変える
2. フェード効果の範囲を調整する
3. 中心の白い円のサイズを変更する
4. 異なる色の組み合わせで別の天体を表現する

## 次章への準備
Chapter 1で学習した内容：
- UV座標システム
- 距離計算 (`length()`)
- 角度計算 (`atan2()`)
- 条件分岐による領域分け
- 三角関数によるパターン生成
- 色のブレンド (`mix()`)
- 滑らかなグラデーション (`smoothstep()`)

これらの知識を活用して、Chapter 2では時間を使ったアニメーションに挑戦していきます！

## WGSL関数の参考
```wgsl
// グラデーション関数
smoothstep(edge0, edge1, x)  // edge0からedge1の間で滑らかな補間
mix(a, b, t)                 // aとbをtの比率で混合

// 数学関数
length(v)      // ベクトルの長さ
atan2(y, x)    // アークタンジェント
sin(x)         // サイン関数
step(edge, x)  // ステップ関数

// 座標関数
NormalizedCoords(pos)  // 正規化座標を取得
ToLinearRgb(color)     // 線形RGB色空間に変換
```